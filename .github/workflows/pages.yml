name: Catalog origin

# Publishes the install and uninstall scripts, and the module catalog generated
# from the tags that exist, to GitHub Pages, so the command in
# the README points at a stable URL that carries no branch name and can be
# repointed at a WSO2-owned domain later without changing what users have already
# copied and shared.
#
# What is served is what is in this repository at the commit that ran: the scripts
# are versioned here and published from here, so there is one copy to review.
#
# This needs Pages enabled for the repository with GitHub Actions as its source,
# which is a repository setting rather than something this file can turn on. Until
# it is, this workflow fails at the deploy step and the release page remains the
# way to install.
on:
  push:
    branches:
      - main
    paths:
      - 'scripts/install.sh'
      - 'scripts/install.ps1'
      - 'scripts/uninstall.sh'
      - 'scripts/uninstall.ps1'
      - '.github/workflows/pages.yml'
      - 'scripts/assemble-site.sh'
      - 'scripts/site/**'
      - 'internal/catalog/**'
      - 'internal/release/**'
      - 'cmd/wso2-catalog/**'
      - 'cmd/wso2-catalog-input/**'
  workflow_dispatch:

permissions:
  contents: read

# One deployment at a time, and a queued one is not cancelled: the last push to
# main is what should end up published, not whichever run finished last.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  publish:
    name: Publish to Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    permissions:
      # What deploying to Pages requires, and nothing else. `contents` stays
      # read: the catalog is generated from tags and from released assets, and
      # both are read.
      pages: write
      id-token: write
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Every module tag has to be present: this deployment replaces the
          # whole site, so it regenerates the catalog rather than publishing a
          # site without one.
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.25.x'
          cache-dependency-path: |
            go.mod
            sdk/go.mod

      # A run that published an unrunnable script would break the one command the
      # README tells people to use, so the shell script is parsed before it goes
      # out. The PowerShell script is proven by the Windows job on every pull
      # request, which is a stronger check than anything available here.
      - name: Check the scripts parse
        run: |
          set -euo pipefail
          bash -n scripts/install.sh
          bash -n scripts/uninstall.sh

      - name: Assemble the site
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # The command the page tells people to run; the same repository
          # variable names the released binary (release.yml).
          CLI_NAME: ${{ vars.CLI_NAME || 'ws' }}
        run: ./scripts/assemble-site.sh site

      - name: Configure Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

      - name: Upload the site
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: site

      - name: Deploy to Pages
        id: deploy
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
