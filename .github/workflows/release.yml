name: Release

# A version tag is the whole trigger. Nothing publishes from a branch, so an
# artifact on the release page always corresponds to a tag someone pushed.
on:
  push:
    tags:
      - 'v*'

# Read-only by default. Only the job that publishes the release is granted write
# access to repository contents, and nothing here is granted anything else.
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # A tag may not ship artifacts that fail the proof. This is the same gate a
  # pull request runs and the same script a contributor runs locally, so the
  # release is held to the standard the repository already enforces rather than
  # to a second, weaker one.
  acceptance:
    name: Architecture Proof
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.25.x'
          cache-dependency-path: |
            go.mod
            sdk/go.mod

      - name: Run the architecture-proof acceptance gate
        run: ./scripts/acceptance.sh

  release:
    name: Publish artifacts
    needs: acceptance
    runs-on: ubuntu-latest
    permissions:
      # Creating the release and uploading its assets. GoReleaser needs nothing
      # further: this release publishes no container image, no Homebrew tap, and
      # no package repository.
      contents: write
    env:
      # The command the shell is published as. GoReleaser names the binary and
      # the shell's own text after it; the repository variable CLI_NAME renames
      # it without a code change.
      CLI_NAME: ${{ vars.CLI_NAME || 'ws' }}
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # GoReleaser reads the tag history to determine the version and to
          # build the changelog, and a shallow clone leaves it with neither.
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.25.x'
          cache-dependency-path: |
            go.mod
            sdk/go.mod

      # The help page names every product this release knows about, offline, so
      # the release carries a copy of the published catalog index (ADR 0015).
      # It is the published document rather than one generated here, the way
      # released-shell-protocols.sh reads the published shell: help must never
      # advertise a product wso2 product install would then fail to find. The
      # origin is catalog.DefaultOrigin, which a catalog test holds this URL to.
      #
      # The copy is proven to read back before anything is published: a shell
      # built with it here must list every product it names. An unreachable,
      # unreadable or empty index fails the release rather than shipping a help
      # page that names nothing.
      - name: Fetch and check the published catalog index
        shell: bash
        run: |
          set -euo pipefail
          index="$(curl -fsSL --retry 3 https://wso2.github.io/wso2-cli/index.json)"
          encoded="$(printf '%s' "${index}" | base64 -w0)"
          go build -trimpath \
            -ldflags "-X github.com/wso2/wso2-cli/internal/catalog.releasedIndex=${encoded}" \
            -o "${RUNNER_TEMP}/wso2-catalog-check" ./cmd/wso2
          ./scripts/help-lists-released-catalog.sh "${RUNNER_TEMP}/wso2-catalog-check" "${encoded}"
          echo "WSO2_RELEASED_CATALOG=${encoded}" >>"${GITHUB_ENV}"

      - name: Build and publish the release
        uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7.2.3
        with:
          # Pinned in step with the Makefile's own pin, so a contributor running
          # `make release-snapshot` builds with the version that publishes.
          version: v2.17.1
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Proves what a user will actually find, by fetching it back from the
      # release page rather than inspecting the build output: the checksum file
      # describes every archive published, the archive named for this tag
      # extracts to a binary reporting this tag's version, and the protocol
      # window it claims is the one the shell's own source declares.
      #
      # A release that ships a binary still reporting the development
      # placeholder, or a checksum file that does not cover every archive
      # beside it, fails here rather than reaching a user.
      - name: Verify the published artifacts
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          tag="${GITHUB_REF_NAME}"
          archive="wso2-cli-${tag}-linux-amd64.tar.gz"

          published="$(mktemp -d)"
          gh release download "${tag}" --dir "${published}" --repo "${GITHUB_REPOSITORY}"

          cd "${published}"

          # What the install scripts will trust has to be what was built. A
          # checksum file rewritten or truncated between build and publish is
          # exactly what this comparison catches.
          if ! diff -u "${GITHUB_WORKSPACE}/dist/checksums.txt" checksums.txt; then
            echo "::error::the published checksums.txt is not the one that was built"
            exit 1
          fi

          # --ignore-missing exits non-zero when it verifies nothing, so this
          # cannot pass on an empty file. What it cannot see is an archive that
          # is present but unlisted, so the two sets of names are compared as
          # well: a target dropped from the checksum file would otherwise ship
          # unverifiable. Names rather than counts, because a duplicated line
          # keeps the count right while leaving another archive uncovered.
          sha256sum --check --ignore-missing checksums.txt
          find . -maxdepth 1 -type f \( -name '*.tar.gz' -o -name '*.zip' \) \
            -printf '%f\n' | sort >published-names.txt
          awk '{ name = $2; sub(/^\*/, "", name); print name }' checksums.txt | sort >listed-names.txt
          if ! diff -u published-names.txt listed-names.txt; then
            echo "::error::the published archives and the checksum file do not describe the same set"
            exit 1
          fi
          echo "$(wc -l <published-names.txt) archives published and all verified"

          # The state root is redirected so that reading the module inventory
          # cannot touch the runner's real home directory.
          workdir="$(mktemp -d)"
          state="$(mktemp -d)"
          export WSO2_HOME="${state}"
          tar -xzf "${archive}" -C "${workdir}"

          if [[ ! -x "${workdir}/${CLI_NAME}" ]]; then
            echo "::error::the archive holds no ${CLI_NAME} binary"
            exit 1
          fi
          reported="$("${workdir}/${CLI_NAME}" version)"
          echo "${reported}"

          # Its help page names commands after the command users will type.
          if ! "${workdir}/${CLI_NAME}" help | grep -qF "${CLI_NAME} <command>"; then
            echo "::error::the released binary does not name its commands ${CLI_NAME}"
            exit 1
          fi

          if ! grep -qF "${tag}" <<<"${reported}"; then
            echo "::error::the released binary reports no version matching ${tag}"
            exit 1
          fi
          if grep -qF '0.0.0-dev' <<<"${reported}"; then
            echo "::error::the released binary reports the development placeholder"
            exit 1
          fi

          # A release build injects no protocol versions: the shell speaks the
          # window declared in sdk/protocol, the current version and its
          # predecessor. A build that narrowed it would cut off module releases
          # for users a generation behind, so the released binary is asked what
          # it speaks and held to what the source declares.
          #
          # The whole field is read, not its first word, because the window is
          # a list. Both are required to be non-empty: if the version output
          # ever stops carrying a Protocol field under this label, an unguarded
          # comparison would find two empty strings equal and report agreement
          # it never saw.
          protocol_field='$1 == "Protocol" { sub(/^Protocol[[:space:]]+/, ""); print }'
          released_protocol="$(awk "${protocol_field}" <<<"${reported}")"
          source_protocol="$(cd "${GITHUB_WORKSPACE}" && go run ./cmd/wso2 version | awk "${protocol_field}")"
          if [[ -z "${released_protocol}" || -z "${source_protocol}" ]]; then
            echo "::error::no protocol version was reported, so nothing was compared"
            exit 1
          fi
          if [[ "${released_protocol}" != "${source_protocol}" ]]; then
            echo "::error::released protocol ${released_protocol} does not match the declared window ${source_protocol}"
            exit 1
          fi
          echo "Protocol ${released_protocol} matches the declared window"

          # The same read-back the copy passed before the build, asked of the
          # published binary: it proves GoReleaser injected the copy, not only
          # that the copy was sound.
          "${GITHUB_WORKSPACE}/scripts/help-lists-released-catalog.sh" "${workdir}/${CLI_NAME}" "${WSO2_RELEASED_CATALOG}"
